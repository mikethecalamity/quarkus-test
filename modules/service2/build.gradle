plugins {
    id 'io.quarkus'
}

description = 'Service 2'
version = '1.0.0'

dependencies {
    runtimeOnly project(':service1')
    runtimeOnly project(':plugin2')

    runtimeOnly 'io.quarkus:quarkus-resteasy'
}
